<li>
  <%# Simple URL stored in photo.image %>
  <%= image_tag photo.image, width: 200 if photo.image.present? %><br>

  <%= photo.caption.presence || "(no caption)" %><br>
  posted by <%= link_to photo.owner.username, user_path(photo.owner) %>
  <br>
</li>
